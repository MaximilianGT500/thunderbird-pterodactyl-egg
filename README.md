# thunderbird-pterodactyl-egg
This Pterodactyl Egg runs Mozilla Thunderbird as a GUI desktop application inside a container and exposes it via WebVNC (noVNC). You can use Thunderbird directly in your browser, without installing a native VNC client.
